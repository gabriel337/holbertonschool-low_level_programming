#include <stdio.h>

/**
 * main - prints lower case alphabet without q and e letters
 * Return: returns 0
 */

int main(void)
{
char al;

for (al = 'a'; al <= 'z'; al++)
{
putchar (al);
}
return (0);
}
