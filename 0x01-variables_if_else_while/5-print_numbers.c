#include <stdio.h>

/**
 * main - will return if the number is - + or == to n
 * Return: returns 0
*/
int main(void)
{
int x;
for (x = '0'; x <= '9'; x++)
{
putchar(x);
}
putchar('\n');
return (0);
}
